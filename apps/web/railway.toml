[build]
builder = "dockerfile"
# Relative from apps/web/ (Railway rootDirectory setting)
dockerfilePath = "../../Dockerfile.web"

[deploy]
# Empty startCommand to use Dockerfile CMD
startCommand = ""
healthcheckPath = "/"
healthcheckTimeout = 10000
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
