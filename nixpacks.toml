# Force npm as package manager (not pnpm)
providers = []

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run db:migrate:prod && node dist/apps/api/main.js"
