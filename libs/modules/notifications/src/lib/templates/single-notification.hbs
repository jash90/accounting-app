<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #1f2937;
      margin: 0;
      padding: 0;
      background-color: #f3f4f6;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background: #ffffff;
    }
    .header {
      background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
      color: #ffffff;
      padding: 32px 24px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 24px;
      font-weight: 600;
    }
    .header .type-badge {
      display: inline-block;
      margin-top: 12px;
      padding: 4px 12px;
      background: rgba(255, 255, 255, 0.2);
      border-radius: 16px;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .content {
      padding: 32px 24px;
    }
    .greeting {
      font-size: 16px;
      color: #374151;
      margin-bottom: 24px;
    }
    .message-box {
      background: #f9fafb;
      border-left: 4px solid #2563eb;
      padding: 16px 20px;
      margin: 24px 0;
      border-radius: 0 8px 8px 0;
    }
    .message-box p {
      margin: 0;
      color: #4b5563;
    }
    .actor-info {
      display: flex;
      align-items: center;
      margin: 20px 0;
      padding: 12px 16px;
      background: #eff6ff;
      border-radius: 8px;
    }
    .actor-info .avatar {
      width: 40px;
      height: 40px;
      background: #2563eb;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #ffffff;
      font-weight: 600;
      margin-right: 12px;
    }
    .actor-info .name {
      font-weight: 500;
      color: #1f2937;
    }
    .actor-info .label {
      font-size: 12px;
      color: #6b7280;
    }
    .cta-button {
      display: inline-block;
      padding: 14px 28px;
      background: #2563eb;
      color: #ffffff !important;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 500;
      margin: 24px 0;
      text-align: center;
    }
    .cta-button:hover {
      background: #1d4ed8;
    }
    .footer {
      background: #f9fafb;
      padding: 24px;
      text-align: center;
      border-top: 1px solid #e5e7eb;
    }
    .footer p {
      margin: 4px 0;
      font-size: 12px;
      color: #9ca3af;
    }
    .footer .logo {
      font-weight: 600;
      color: #2563eb;
      font-size: 14px;
      margin-bottom: 8px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>{{title}}</h1>
      <span class="type-badge">{{typeLabel}}</span>
    </div>
    
    <div class="content">
      <p class="greeting">Witaj {{recipientName}},</p>
      
      {{#if message}}
      <div class="message-box">
        <p>{{message}}</p>
      </div>
      {{/if}}
      
      {{#if actorName}}
      <div class="actor-info">
        <div class="avatar">{{actorInitials}}</div>
        <div>
          <div class="name">{{actorName}}</div>
          <div class="label">Wykonał tę akcję</div>
        </div>
      </div>
      {{/if}}
      
      {{#if actionUrl}}
      <div style="text-align: center;">
        <a href="{{actionUrl}}" class="cta-button">Zobacz szczegóły</a>
      </div>
      {{/if}}
    </div>
    
    <div class="footer">
      <div class="logo">AppTax</div>
      <p>Ta wiadomość została wysłana automatycznie.</p>
      <p>{{timestamp}}</p>
    </div>
  </div>
</body>
</html>
