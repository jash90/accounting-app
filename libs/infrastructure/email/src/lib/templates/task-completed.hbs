<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Zadanie zakończone</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #16a34a; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
    .header-icon { font-size: 24px; margin-right: 10px; }
    .content { background: #f9fafb; padding: 20px; border: 1px solid #e5e7eb; border-top: none; border-radius: 0 0 8px 8px; }
    .field { margin-bottom: 12px; }
    .label { font-weight: 600; color: #6b7280; font-size: 12px; text-transform: uppercase; }
    .value { font-size: 16px; margin-top: 4px; }
    .success-badge { display: inline-block; padding: 4px 12px; border-radius: 4px; font-size: 14px; font-weight: 600; background: #dcfce7; color: #16a34a; }
    .footer { margin-top: 20px; font-size: 12px; color: #9ca3af; text-align: center; }
  </style>
</head>
<body>
  <div class="header">
    <h1 style="margin: 0;"><span class="header-icon">✅</span> Zadanie zakończone</h1>
  </div>
  <div class="content">
    <div class="field">
      <div class="label">Tytuł zadania</div>
      <div class="value">{{taskTitle}}</div>
    </div>
    {{#if clientName}}
    <div class="field">
      <div class="label">Klient</div>
      <div class="value">{{clientName}}</div>
    </div>
    {{/if}}
    <div class="field">
      <div class="label">Status</div>
      <div class="value"><span class="success-badge">Zakończone</span></div>
    </div>
    <div class="field">
      <div class="label">Priorytet</div>
      <div class="value">{{priorityLabel}}</div>
    </div>
    {{#if assigneeName}}
    <div class="field">
      <div class="label">Wykonane przez</div>
      <div class="value">{{assigneeName}}</div>
    </div>
    {{/if}}
    {{#if dueDate}}
    <div class="field">
      <div class="label">Termin</div>
      <div class="value">{{dueDate}}</div>
    </div>
    {{/if}}
    <div class="field">
      <div class="label">Firma</div>
      <div class="value">{{companyName}}</div>
    </div>
    <div class="field">
      <div class="label">Zakończone przez</div>
      <div class="value">{{performedByName}}</div>
    </div>
    <div class="field">
      <div class="label">Data zakończenia</div>
      <div class="value">{{completedAt}}</div>
    </div>
  </div>
  <div class="footer">
    Ta wiadomość została wygenerowana automatycznie przez system Accounting.
  </div>
</body>
</html>
